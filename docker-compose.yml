services:
  discord-bot:
    build: .
    image: blindy42:v1
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data

  # To test prod container
  # discord-bot:
  #   image: ghcr.io/42parispedago/blindbot:1.0.0
  #   restart: unless-stopped
  #   env_file:
  #     - .env
  #   volumes:
  #     - ./data:/app/data  